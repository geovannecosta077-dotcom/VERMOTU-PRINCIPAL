{
  "buildCommand": "cd ../.. && pnpm run typecheck:libs && cd artifacts/motohub && pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
